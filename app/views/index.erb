<body>
  <div class="header">
    <h1> Links </h1>

    <form action="/add">
      <button type="submit">Add new link</button>
    </form>

  </div>
  <div class='main'>
    <% @links.each do |link| %>
      <div class="bookmark">
        <a href="<%=link.url%>"><%= link.title %> </a>
        <p>Tags: <%=@bookmark.tags_to_string(link.tags)%></p>
      </div>
    <% end %>
  </div>
<body>


<style>

  body {

  }

  .bookmark {
    margin: 5px;
    padding: 20px;
    background-color: #ffffe6;
    width: 500px;
  }

</style>
