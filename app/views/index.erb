<h1> Links </h1>

<ul id='links'>
  <% @links.each do |link| %>
    <div class="entry">
      <a href="<%=link.url%>"><%= link.title %> </a>
      <p>Tags: <%=link.print_tags%></p>
    </div>
  <% end %>
</ul>
