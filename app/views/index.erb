<h1> Links </h1>

<ul id='links'>
  <% @links.each do |link| %>
    <div class="entry">
      <a href="<%=link.url%>"><%= link.title %> </a>
      <p>Tags: <%=link.tags_to_string%></p>
    </div>
  <% end %>
</ul>
